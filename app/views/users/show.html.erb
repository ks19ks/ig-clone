<div class="text-center">
  <h1><%= @user.name %>'s page</h1>
  <p><%= image_tag @user.image.url, class: 'rounded-circle img-thumbnail' %></p>
  <p>E-mail: <%= @user.email %></p>
  <p><%= link_to 'Edit', edit_user_path(@user) %></p>
</div>
